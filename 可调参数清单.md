# 可调参数清单

> 本文档记录训练代码中所有**可能需要回头调整**的参数，集中管理。
> 所有参数都定义在 `config.py` 中，本文档说明每个参数的位置、含义、调整建议。
> 
> 修改时直接编辑 `config.py`，**不要在其他文件里硬编码**。

---

## 一、数据集划分

| 参数 | config.py 中的变量 | 默认值 | 调整建议 |
|---|---|---|---|
| 训练集物体列表 | `TRAIN_OBJECTS` | 9个英文名 | 实际采集后按 `meta.json` 的 `object_class` 字段补齐/修正 |
| 测试集物体列表 | `TEST_OBJECTS` | `[]` 占位 | 等测试集物体采集完成后填入 |
| 验证集比例 | `VAL_RATIO` | `0.2` | 数据量足够大可以减到 0.15 |
| 划分随机种子 | `SPLIT_SEED` | `42` | 一般不动 |

---

## 二、触觉数据处理

| 参数 | config.py 中的变量 | 默认值 | 调整建议 |
|---|---|---|---|
| 三指空载基线 | `TACTILE_BASELINE_PER_FINGER` | `[5988, 5994, 5992]` | **如果传感器换了或长期使用偏移**，需要重新测试空载值更新 |
| 触觉频率 | `TACTILE_FREQ` | `50.0` Hz | 硬件参数，不动 |
| 窗口锚点 | `WINDOW_ANCHOR` | `'lifted'` | 可以改 `'contact'` 取接触瞬间 |
| 窗口偏移 | `WINDOW_OFFSET_SEC` | `0.0` | 锚点后多久开始截，比如 0.3 跳过过渡段 |
| 窗口长度 | `WINDOW_DURATION_SEC` | `1.0` | 加长能拿到更稳定数据，缩短增加样本多样性 |
| 聚合方式 | `TACTILE_AGG_METHODS` | `['mean','max','std']` | 可以只用 mean 或加 min |
| **激活点阈值** | `ACTIVATION_THRESHOLD` | `50.0` | **重要**：影响 `contact_ratio` 物理量。实测数据看到激活点偏少，可降到 30 或 20 |

---

## 三、视觉数据处理

| 参数 | config.py 中的变量 | 默认值 | 调整建议 |
|---|---|---|---|
| 用 before 还是 after | `VISION_TIMING` | `'before'` | 可改 `'after'` 试 |
| 输入分辨率 | `VISION_INPUT_SIZE` | `224` | ResNet18 标准值，一般不动 |
| 深度归一化范围 | `DEPTH_MIN_MM` / `DEPTH_MAX_MM` | `200 / 1000` | **实测物体高度对应深度大约 350-550mm**，可缩紧到 `300 / 700` 提高细节区分度 |

---

## 四、网络结构

| 参数 | config.py 中的变量 | 默认值 | 调整建议 |
|---|---|---|---|
| 视觉特征维度 | `VISUAL_FEAT_DIM` | `256` | 减小到 128 防过拟合 |
| 触觉特征维度 | `TACTILE_FEAT_DIM` | `128` | 一般不动 |
| 融合维度 | `FUSION_DIM` | `256` | 配合视觉维度调 |
| Pv 维度 | `PV_DIM` | `4` | 不动（物理量个数固定） |
| Pt 维度 | `PT_DIM` | `6` | 不动 |
| 是否预训练 ResNet18 | `USE_PRETRAINED_RESNET` | `True` | 数据量足够时可设 False |

---

## 五、训练超参（最常调）

| 参数 | config.py 中的变量 | 默认值 | 调整建议 |
|---|---|---|---|
| **学习率** | `LEARNING_RATE` | `1e-3` | 小数据集用 1e-3，效果不稳定降到 5e-4 或 1e-4 |
| Weight Decay | `WEIGHT_DECAY` | `1e-4` | 过拟合时增到 5e-4 |
| **Batch size** | `BATCH_SIZE` | `32` | 显存够可加到 64 |
| **最大 Epochs** | `EPOCHS` | `50` | 数据少可减到 30 |
| Scheduler T_max | `LR_SCHEDULER_T_MAX` | `50` | 与 EPOCHS 同步 |
| 损失权重-success | `LOSS_WEIGHT_SUCCESS` | `1.0` | 二分类 BCE 损失权重 |
| 损失权重-failure | `LOSS_WEIGHT_FAILURE` | `1.0` | 5分类 CE 损失权重 |
| **早停 patience** | `EARLY_STOP_PATIENCE` | `10` | val loss 连续 N 次不降则停 |
| 早停 min_delta | `EARLY_STOP_MIN_DELTA` | `1e-4` | val loss 下降量阈值 |
| DataLoader workers | `NUM_WORKERS` | `4` | 训练机性能强可加到 8 |
| 保存间隔 | `SAVE_EVERY_N_EPOCHS` | `5` | |
| 保留最佳 N 个 | `KEEP_BEST_N` | `3` | |

---

## 六、数据增强（训练时实时做）

| 参数 | config.py 中的变量 | 默认值 | 调整建议 |
|---|---|---|---|
| 启用增强 | `AUG_ENABLED` | `True` | 调试时可关 |
| RGB 颜色抖动 brightness | `AUG_BRIGHTNESS` | `0.2` | 0.1-0.3 |
| RGB 颜色抖动 contrast | `AUG_CONTRAST` | `0.2` | 0.1-0.3 |
| RGB 颜色抖动 saturation | `AUG_SATURATION` | `0.2` | 0.1-0.3 |
| 随机旋转角度范围 | `AUG_ROTATION_DEG` | `5.0` | ±5°，可放宽到 ±10° |

---

## 七、Pose 修正规则（推理后处理）

> 这部分**不在训练代码里**，定义在未来的 `inference.py` 中。
> 训练阶段不影响，只是部署时用到。

| 参数 | 含义 | 默认值 | 调整建议 |
|---|---|---|---|
| `POSE_ALPHA` | radial_offset 时力心→位移的比例 | `0.5` | 实测重抓成功率决定 |
| `POSE_DELTA_Z_SHALLOW` | too_shallow 时 z 修正 | `-0.02` m | 改太深则减小绝对值 |
| `POSE_DELTA_Z_DEEP` | too_deep 时 z 修正 | `+0.02` m | 同上 |
| `POSE_Z_MIN_LIMIT` | z 安全下限 | `0.204` m | 硬件约束，不能违反 |
| `POSE_MAX_RETRIES` | 最多重抓次数 | `3` | 论文方案设定 |

---

## 八、当前未决 / 待回头确认的事项

> 这部分记录"现在用了一个临时值，将来需要验证"的参数。

### 8.1 激活点阈值实测
- **现状**：默认 `ACTIVATION_THRESHOLD = 50`
- **实测发现**：之前上传的 sample 在阈值 50 下 `contact_ratio` 只有 0.04（9/225 点激活），偏少
- **后续动作**：等数据集采齐后，统计 5 类样本的 `contact_ratio` 分布，找一个能拉开类间差距的阈值（候选 20、30）

### 8.2 深度归一化范围
- **现状**：默认 `[200, 1000]` mm
- **可能问题**：范围过宽，归一化后物体区域只占 0.1-0.3 区间，分辨率不足
- **后续动作**：等数据采齐后，统计所有 sample 深度图的有效范围，缩紧到合适区间

### 8.3 损失类别权重
- **现状**：训练时根据各类样本数自动计算（在 train.py 中实现）
- **可能问题**：too_shallow / too_deep 样本极少（预估各 ~6 个），权重过大可能导致过拟合
- **后续动作**：若小类训练不稳定，改为方案 C（合并 too_shallow+too_deep）

### 8.4 视觉分支 4 通道适配
- **现状**：第一层 conv 改成 4 通道，前 3 通道复用预训练，第 4 通道随机初始化
- **可能问题**：如果第 4 通道（depth）训练初期梯度不稳定
- **后续动作**：必要时可改为"RGB 走预训练 ResNet18，Depth 走单独小 CNN"的双分支结构

### 8.5 Pose 头是否回归
- **现状**：已砍掉，改为推理后处理
- **后续可能性**：若实验效果好且评审要求，可加回 pose 回归头（用规则伪标签）

---

## 九、修改流程

1. 直接打开 `config.py` 修改对应变量
2. 如果改的是**预处理参数**（如 ACTIVATION_THRESHOLD、WINDOW_ANCHOR、DEPTH 范围、TACTILE_BASELINE），需要重跑 `python preprocess.py` 生成新的 `cache.npz`
3. 如果改的是**训练参数**（LR、batch、epoch、增强），直接重跑 `python train.py`，不用重新预处理
4. 如果改的是**网络结构**（VISUAL_FEAT_DIM、TACTILE_FEAT_DIM、FUSION_DIM 等），需要重跑 train.py，旧 checkpoint 无效

---

## 十、版本记录

| 日期 | 修改人 | 修改内容 |
|---|---|---|
| 2026-06-15 | 初版 | 初始建档，记录与训练代码 chat 讨论的所有决策 |
